# --- Application Configuration ---

# The port the Uvicorn server should listen on.
# This will be read by the app and used by the Dockerfile CMD.
APP_PORT=8000

# The base URL of the application for generating full short links (optional).
# If not set, relative paths will be used or you can manually construct.
# BASE_URL=http://localhost:8000
